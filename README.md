# Cross_language-Code-clone-detection
This project is a complete system for cross-language code clone detection. By constructing an automated data preprocessing pipeline, the system can parse source code in multiple programming languages into abstract syntax trees and extract key control flow subtrees, then utilize deep learning models to learn structured features.
